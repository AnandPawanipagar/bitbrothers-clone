import React from "react";

const WhatDrivesUs = () => {
  return (
    <div className="flex flex-col items-center lg:mx-[12em] mb-[4em]">
      <div className="text-[2.35em] font-noraml">What drives us</div>
      <p className="mt-6 font-normal text-[#808080] text-[1.35em] leading-[1.68em] text-center">
        This is a declaration of the core values that guide everything we do as
        an organization. Simply put, the Manifesto is the bedrock upon which the
        entire organization is built, and without it, Bit Brothers wouldn’t be
        Bit Brothers.
      </p>
    </div>
  );
};

export default WhatDrivesUs;
